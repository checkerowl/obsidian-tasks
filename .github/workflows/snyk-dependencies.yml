name: Snyk Security SCA Scan 
permissions:
  contents: read
  pull-requests: write
  
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@main

      - name: Run Snyk to check for code vulnerabilities in dependencies
        uses: snyk/actions/node@master
        with:
          args: 'test --all-dependencies'
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
